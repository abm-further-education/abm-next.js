import { CourseDetailInfo } from '@/domains/courses/components/CourseDetail';

export const courseDetailsData_sp: { [key: string]: CourseDetailInfo } = {
  // ... (copy all data from courseDetails.ts here, but all string values translated to Spanish)
};
